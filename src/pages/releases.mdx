---
title: AstroEco is Releasing...
description: 'Example: Displaying GitHub releases using astro-loader-github-releases'
subtitle: ''
bgType: plum
toc: false
ogImage: true
---

import BaseLayout from '~/layouts/BaseLayout.astro'
import StandardLayout from '~/layouts/StandardLayout.astro'
import GithubView from '~/components/views/GithubView.astro'

export const githubActivityEnabled = !!import.meta.env.GITHUB_TOKEN

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  bgType={frontmatter.bgType}
  ogImage={frontmatter.ogImage}
>
  <StandardLayout isCentered={true} isWideMargin={true}>
    <Fragment slot="head">
      <h1 class="ml-12 max-sm:ml-0"
      >AstroEco is <span class="animate-pulse">Releasing...</span></h1>
      <p class="!mt-[-1rem] italic opacity-50"
      >Example: Displaying GitHub releases using <a href="https://github.com/lin-stephanie/astro-loaders/tree/main/packages/astro-loader-github-releases" target="_blank">astro-loader-github-releases</a></p>
    </Fragment>
    {githubActivityEnabled ? (
      <GithubView slot="github" collectionType="releases" />
    ) : (
      <div slot="github" class="mt-16 border border-[var(--c-border)] p-6 text-center font-mono">
        <p class="m-0 text-sm text-[var(--c-text)]">
          GitHub releases are an optional build-time integration.
        </p>
        <p class="mb-0 mt-3 text-xs text-[var(--c-text-light)]">
          Set <code>GITHUB_TOKEN</code> during build to fetch release data.
        </p>
      </div>
    )}
  </StandardLayout>
</BaseLayout>
