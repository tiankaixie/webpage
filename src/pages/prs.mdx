---
title: GitHub Pull Requests
description: 'Optional build-time GitHub pull request activity feed.'
subtitle: ''
bgType: plum
toc: false
ogImage: true
---

import BaseLayout from '~/layouts/BaseLayout.astro'
import StandardLayout from '~/layouts/StandardLayout.astro'
import GithubView from '~/components/views/GithubView.astro'

export const githubActivityEnabled = !!import.meta.env.GITHUB_TOKEN

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  bgType={frontmatter.bgType}
  ogImage={frontmatter.ogImage}
>
  <StandardLayout isCentered={true} isWideMargin={true}>
    <Fragment slot="head">
      <h1 class="ml-12 max-sm:ml-0">GitHub Pull Requests</h1>
      <p class="!mt-[-1rem] italic opacity-50"
      >Optional build-time activity feed using <a href="https://github.com/lin-stephanie/astro-loaders/tree/main/packages/astro-loader-github-prs" target="_blank">astro-loader-github-prs</a></p>
    </Fragment>
    {githubActivityEnabled ? (
      <GithubView slot="github" collectionType="prs" />
    ) : (
      <div slot="github" class="mt-16 border border-[var(--c-border)] p-6 text-center font-mono">
        <p class="m-0 text-sm text-[var(--c-text)]">
          GitHub pull requests are an optional build-time integration.
        </p>
        <p class="mb-0 mt-3 text-xs text-[var(--c-text-light)]">
          Set <code>GITHUB_TOKEN</code> during build to fetch pull request data.
        </p>
      </div>
    )}
  </StandardLayout>
</BaseLayout>
